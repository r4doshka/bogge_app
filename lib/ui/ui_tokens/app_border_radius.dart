import 'package:flutter/material.dart';

class AppBorderRadius {
  AppBorderRadius._();

  // Radius tokens
  static const r8 = Radius.circular(8);
  static const r12 = Radius.circular(12);
  static const r16 = Radius.circular(16);
  static const r20 = Radius.circular(20);
  static const r24 = Radius.circular(24);
  static const r28 = Radius.circular(28);
  static const r32 = Radius.circular(32);
  static const r100 = Radius.circular(100);

  // BorderRadius (all)
  static const all8 = BorderRadius.all(r8);
  static const all12 = BorderRadius.all(r12);
  static const all16 = BorderRadius.all(r16);
  static const all20 = BorderRadius.all(r20);
  static const all24 = BorderRadius.all(r24);
  static const all28 = BorderRadius.all(r28);
  static const all32 = BorderRadius.all(r32);

  static const all100 = BorderRadius.all(r100);
}
